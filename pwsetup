#!/bin/rc

# desc: choose password

switch($1){
case checkdone
	if(~ $#password 1)
		pwsetup=done
	if not
		pwsetup=ready
	export pwsetup
case go
	echo
	echo 'Setup Password'
	echo

	options=`{ls -p /n/newfs/adm/timezone | grep -v -e '(README|local)'}
	prompt 'Password: ' $password
	
	export password
}

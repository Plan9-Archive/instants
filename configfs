#!/bin/rc

# desc: choose the type of file system to install

switch($1){
case checkdone
	configfs=done
	export configfs

case go
	echo 'Advanced Namespace Tools uses the Fossil file server backed by Venti archival data store'
	echo
	fstype=fossil
	export fstype
}
